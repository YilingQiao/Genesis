{
  "name": "genesis-web-gui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "generate:types": "python -m genesis.vis.web.protocol --export-schema src/types/protocol.schema.json && npx json2ts -i src/types/protocol.schema.json -o src/types/protocol.generated.d.ts --unreachableDefinitions"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.3",
    "json-schema-to-typescript": "^15.0.4",
    "vite": "^6.3.5",
    "vitest": "^3.2.1"
  }
}
